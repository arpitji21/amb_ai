# Leave unset to run the dashboard against its built-in in-browser simulation.
# Set to your FastAPI backend's WebSocket endpoint to use real server-driven data.
VITE_WS_URL=ws://localhost:8000/ws/ambulances
VITE_API_URL=http://localhost:8000
